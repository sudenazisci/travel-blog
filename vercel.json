{
  "buildCommand": "[ -d \"frontend\" ] && (cd frontend && npm install && npm run build) || npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
